# Suppress lifecycle scripts from installed packages (postinstall malware defense).
# Note: this also suppresses prepublishOnly — release.yml runs `npm run build` explicitly.
ignore-scripts=true

# Fail CI on moderate or higher severity vulnerabilities.
audit-level=moderate

# Pin new dependencies to exact versions (CLI, not a library).
save-exact=true
